1. Differrence between ClassNotFoundException and NoClassDefFoundError ?
	ClassNotFoundException occurs when classpath does not get updated with required JAR files while error occurs when the required class definition is not present at runtime.
	
2. How can we decide that custom exception should be checked or unchecked in java?
	If you want to write a checked exception that is automatically enforced by the Handle or Declare Rule, you need to extend the Exception class.
	If you want to write a runtime exception, you need to extend the RuntimeException class.

3. What is the Exception Hierarchy ?
	Ans : Object -> Throwable ->   Exception 
										-> RuntimeException
										-> Other Exceptions
							  ->   Error

4. When we use multi catch block with Exception as the first one is valid or not ? (any compile time exception ?)
5. Why the "Exception" block catches all the other Exceptions?
6. List of Exceptions :: 
	NoSuchMethodError
	CloneNotSupportedException
	ConcurrentModificationException
	UnsupportedOperationException
7. What are the exceptions we get in Hibernate?
	https://www.baeldung.com/hibernate-exceptions#:~:text=Hibernate%20Exception%20Overview&text=These%20can%20be%20mapping%20errors,exceptions%20mostly%20extend%20from%20HibernateException.
	
	These exceptions mostly extend from HibernateException. However, if we're using Hibernate as a JPA persistence provider, these exceptions may get wrapped into PersistenceException
	
	1. MappingException -- parent
		childs: 
			AnnotationException – a problem with an annotation
			DuplicateMappingException – duplicate mapping for a class, table, or property name
			InvalidMappingException – mapping is invalid
			MappingNotFoundException – mapping resource could not be found
			PropertyNotFoundException – an expected getter or setter method could not be found on a class
	2. AnnotationException
	3. QuerySyntaxException
	4. SchemaManagementException
	5. CommandAcceptanceException
	6. Others: 
		ConstraintViolationException
		DataException
		JDBCConnectionException
		LockAcquisitionException
		PessimisticLockException
		QueryTimeoutException
		SQLGrammarException
		GenericJDBCException
-------------------------------------------------------------------------------------------------------------------
8. 